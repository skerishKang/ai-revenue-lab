name: B60 Visual QA Temp

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary QA paused while exact B60 raster assets are repaired."
