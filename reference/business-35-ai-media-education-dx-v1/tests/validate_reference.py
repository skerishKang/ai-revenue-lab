from pathlib import Path
import json,re,sys
r=Path(__file__).resolve().parents[1];h=(r/'index.html').read_text();c=(r/'styles/main.css').read_text();j=(r/'scripts/review.js').read_text();s=['cover','diagnostic','curriculum','workshop','workflow','pilot','mobile'];a=sorted((r/'assets/images').glob('*.svg'));m=(r/'IMAGE_SOURCES.md').read_text();labels=['SYNTHETIC ORGANIZATION','CURRENT-STATE EVIDENCE','CONSULTANT INTERPRETATION — SYNTHETIC','SKILL GAP','POLICY CONSTRAINT','TRAINING MODULE','HUMAN FACILITATOR','APPROVED TOOL','PROHIBITED USE','PRACTICE EVIDENCE','WORKFLOW REVISION','PILOT BASELINE','PILOT RESULT — SYNTHETIC','UNRESOLVED ADOPTION RISK','NOT YET ADOPTED','MANAGEMENT DECISION','HUMAN-APPROVED MEDIA DX PLAYBOOK','VISUAL REFERENCE ONLY','NO LIVE CONSULTING OR ORGANIZATION CONNECTION']
checks={'exact_states':re.findall(r'data-state="([^"]+)"',h)==s,'exact_controls':re.findall(r'data-state-control="([^"]+)"',h)==s,'assets':len(a)>=11,'manifest':all(x.name in m for x in a),'labels':all(x in h for x in labels),'local_runtime':not re.search(r'https?://|//[^\s]',h),'version':'mdx-v1-20260729' in h,'responsive':'@media(max-width:560px)' in c,'reduced':'prefers-reduced-motion:reduce' in c,'animationend':'animationend' in j and 'playbookComplete' in j,'reset':"classList.remove('is-running','is-complete')" in j,'no_timeout':'setTimeout' not in j,'evidence_boundary':'CURRENT-STATE EVIDENCE' in h and 'CONSULTANT INTERPRETATION — SYNTHETIC' in h,'capability_boundary':'attendance' in h and 'demonstrated capability' in h,'adoption_boundary':'PILOT RESULT — SYNTHETIC' in h and 'NOT YET ADOPTED' in h}
out={'status':'PASS' if all(checks.values()) else 'FAIL','checks':checks,'asset_count':len(a),'states':s};(r/'evidence/static-self-check.json').write_text(json.dumps(out,ensure_ascii=False,indent=2));print(json.dumps(out,ensure_ascii=False));sys.exit(out['status']!='PASS')
