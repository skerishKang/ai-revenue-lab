(()=>{const panels=[...document.querySelectorAll('[data-state]')],tabs=[...document.querySelectorAll('[data-state-control]')];function select(key){panels.forEach(p=>{const on=p.dataset.state===key;p.hidden=!on;p.classList.toggle('is-active',on)});tabs.forEach(t=>{const on=t.dataset.stateControl===key;t.setAttribute('aria-selected',String(on));t.tabIndex=on?0:-1});history.replaceState(null,'',`#${key}`)}tabs.forEach((t,i)=>{t.addEventListener('click',()=>select(t.dataset.stateControl));t.addEventListener('keydown',e=>{if(!['ArrowRight','ArrowLeft','Home','End'].includes(e.key))return;e.preventDefault();let n=i;if(e.key==='ArrowRight')n=(i+1)%tabs.length;if(e.key==='ArrowLeft')n=(i-1+tabs.length)%tabs.length;if(e.key==='Home')n=0;if(e.key==='End')n=tabs.length-1;tabs[n].focus();select(tabs[n].dataset.stateControl)})});const requested=location.hash.slice(1);select(tabs.some(t=>t.dataset.stateControl===requested)?requested:'cover');const board=document.querySelector('[data-routing-trace]'),replay=document.querySelector('[data-motion-replay]'),status=document.querySelector('[data-motion-status]'),finalRecord=document.querySelector('[data-final-record]');function complete(){board.classList.remove('is-running');board.classList.add('is-complete');board.dataset.motionState='complete';status.textContent='HUMAN-APPROVED MODEL ROUTING POLICY · complete'}function run(){const active=document.activeElement,x=scrollX,y=scrollY;board.classList.remove('is-running','is-complete');board.dataset.motionState='idle';void board.offsetWidth;if(matchMedia('(prefers-reduced-motion: reduce)').matches)complete();else{board.classList.add('is-running');board.dataset.motionState='running';status.textContent='작업에서 사람 승인 모델 라우팅 정책으로 연결 중'}requestAnimationFrame(()=>{scrollTo(x,y);if(active instanceof HTMLElement)active.focus({preventScroll:true})})}replay.addEventListener('click',run);finalRecord.addEventListener('animationend',e=>{if(e.animationName==='routingPolicyComplete'&&board.dataset.motionState==='running')complete()})})();
