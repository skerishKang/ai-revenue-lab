B19 mobile correction: removed the inline three-column provenance grid override from `ux.html`, linked `memory-binding-v2-mobile-fix.css`, clamped the reading workspace to the viewport, kept only the chapter rail horizontally scrollable, and forced provenance/marked-line areas to one column at 390px. This human-authored commit intentionally retriggers the real-Chromium validation after the bot-authored materialization commit.