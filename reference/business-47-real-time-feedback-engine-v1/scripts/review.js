const tabs=[...document.querySelectorAll('[role=tab]')],panels=[...document.querySelectorAll('[role=tabpanel]')];function select(i,f=true){tabs.forEach((t,n)=>{let on=n===i;t.setAttribute('aria-selected',String(on));t.tabIndex=on?0:-1;panels[n].hidden=!on});if(f)tabs[i].focus()}tabs.forEach((t,i)=>{t.addEventListener('click',()=>select(i,false));t.addEventListener('keydown',e=>{let n=i;if(e.key==='ArrowRight')n=(i+1)%tabs.length;else if(e.key==='ArrowLeft')n=(i-1+tabs.length)%tabs.length;else if(e.key==='Home')n=0;else if(e.key==='End')n=tabs.length-1;else if(e.key==='Enter'||e.key===' '){select(i);e.preventDefault();return}else return;e.preventDefault();select(n)})});const replay=document.querySelector('#replay'),panel=document.querySelector('[data-panel=handoff]'),final=document.querySelector('#motion-final');function run(){const y=scrollY,a=document.activeElement;panel.classList.remove('is-replaying');void panel.offsetWidth;if(matchMedia('(prefers-reduced-motion:reduce)').matches){panel.classList.add('is-complete');a?.focus();scrollTo(scrollX,y);return}panel.classList.add('is-replaying');const done=e=>{if(e.target!==final)return;final.removeEventListener('animationend',done);panel.classList.add('is-complete');a?.focus();scrollTo(scrollX,y)};final.addEventListener('animationend',done)}replay?.addEventListener('click',run);window.__B47__={states:tabs.map(t=>t.dataset.state),select,run,version:'b47-20260729-1'};