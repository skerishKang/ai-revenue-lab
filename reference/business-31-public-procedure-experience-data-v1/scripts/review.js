(()=>{const states=[...document.querySelectorAll('[data-state]')];const controls=[...document.querySelectorAll('[data-state-control]')];const stage=document.querySelector('#review-stage');function select(key,focus=false){states.forEach(s=>{const active=s.dataset.state===key;s.hidden=!active;s.classList.toggle('is-active',active)});controls.forEach(c=>{const active=c.dataset.stateControl===key;c.setAttribute('aria-selected',String(active));c.tabIndex=active?0:-1});history.replaceState(null,'',`#${key}`);if(focus) stage.focus({preventScroll:true})}controls.forEach((c,i)=>{c.addEventListener('click',()=>select(c.dataset.stateControl));c.addEventListener('keydown',e=>{if(!['ArrowRight','ArrowLeft','Home','End'].includes(e.key))return;e.preventDefault();let n=i;if(e.key==='ArrowRight')n=(i+1)%controls.length;if(e.key==='ArrowLeft')n=(i-1+controls.length)%controls.length;if(e.key==='Home')n=0;if(e.key==='End')n=controls.length-1;controls[n].focus();select(controls[n].dataset.stateControl)})});const initial=location.hash.slice(1);if(controls.some(c=>c.dataset.stateControl===initial))select(initial);else select('cover');const board=document.querySelector('[data-experience-trace]');const replay=document.querySelector('[data-motion-replay]');const status=document.querySelector('[data-motion-status]');function complete(){board.classList.remove('is-running');board.classList.add('is-complete');board.dataset.motionState='complete';status.textContent='HUMAN-REVIEWED FOLLOW-UP · complete'}function run(){const scrollX=window.scrollX,scrollY=window.scrollY;board.classList.remove('is-running','is-complete');board.dataset.motionState='idle';void board.offsetWidth;if(matchMedia('(prefers-reduced-motion: reduce)').matches){complete()}else{board.classList.add('is-running');board.dataset.motionState='running';status.textContent='공식 절차와 경험 근거 정렬 중'}requestAnimationFrame(()=>window.scrollTo(scrollX,scrollY))}replay.addEventListener('click',run);board.querySelector('.follow-up-seal').addEventListener('animationend',e=>{if(e.animationName==='followUpComplete'&&board.dataset.motionState==='running')complete()});})();
