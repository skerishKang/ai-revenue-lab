from pathlib import Path
import re,json
r=Path(__file__).resolve().parents[1];html=(r/'index.html').read_text();css=(r/'styles/main.css').read_text();js=(r/'scripts/review.js').read_text();states=re.findall(r'data-state="([^"]+)"',html);controls=re.findall(r'data-state-control="([^"]+)"',html);expected=['cover','caller','transcript','interpretation','operator','handoff','mobile'];assets=list((r/'assets/images').glob('*.svg'));labels=['SYNTHETIC EMERGENCY CALL','CALLER SPEECH — SYNTHETIC','MACHINE TRANSCRIPT — UNVERIFIED','INTERPRETATION DRAFT — UNVERIFIED','CRITICAL TERM','NEGATION RISK','NUMBER / LOCATION RISK','LOCATION — UNCONFIRMED','MISHEARING RISK','OPERATOR CLARIFICATION','HUMAN CORRECTION','UNRESOLVED MEANING','NO AUTONOMOUS DISPATCH','NO URGENCY OR THREAT DECISION','HUMAN-VERIFIED BILINGUAL CALL RECORD','VISUAL REFERENCE ONLY','NO LIVE CALL, RECORDING, OR EMERGENCY CONNECTION'];checks={'states':states==expected,'controls':controls==expected,'assets':len(assets)>=11,'labels':all(x in html for x in labels),'local_runtime':not re.search(r'https?://|//cdn',html+css+js),'reduced_motion':'prefers-reduced-motion' in css,'animationend':'animationend' in js and 'callRecordComplete' in js,'no_timeout':'setTimeout' not in js,'version':'eir-v1-20260729' in html,'documented':all(a.name in (r/'IMAGE_SOURCES.md').read_text() for a in assets)};assert all(checks.values()),checks;out={'status':'PASS','checks':checks,'states':states,'assets':len(assets)};(r/'evidence/static-self-check.json').write_text(json.dumps(out,ensure_ascii=False,indent=2));print(json.dumps(out,ensure_ascii=False))
