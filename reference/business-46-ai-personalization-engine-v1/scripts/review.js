const tabs=[...document.querySelectorAll('[role=tab]')],panels=[...document.querySelectorAll('[data-panel]')];function select(i,focus=true){tabs.forEach((t,n)=>{t.setAttribute('aria-selected',n===i);t.tabIndex=n===i?0:-1;panels[n].hidden=n!==i});if(focus)tabs[i].focus()}tabs.forEach((t,i)=>{t.onclick=()=>select(i,false);t.onkeydown=e=>{let n=i;if(e.key==='ArrowRight')n=(i+1)%tabs.length;else if(e.key==='ArrowLeft')n=(i-1+tabs.length)%tabs.length;else if(e.key==='Home')n=0;else if(e.key==='End')n=tabs.length-1;else return;e.preventDefault();select(n)}});const btn=document.querySelector('#replay'),track=document.querySelector('#motionTrack');btn.onclick=()=>{const x=scrollX,y=scrollY,active=document.activeElement;track.classList.remove('run');track.dataset.completionAuthority='pending';void track.offsetWidth;if(matchMedia('(prefers-reduced-motion: reduce)').matches){track.classList.add('run');track.dataset.completionAuthority='reduced-motion-immediate'}else track.classList.add('run');scrollTo(x,y);active?.focus()};track.addEventListener('animationend',e=>{if(e.animationName==='motionFinal')track.dataset.completionAuthority='animationend:motionFinal'});