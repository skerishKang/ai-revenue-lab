# Signals-to-Personalization-Decision-Trace

Sequence: explicit preferences → temporary context → user correction → candidates → included/excluded signals → missing data/fallback → override → final trace. Final authority is actual `animationend` for `motionFinal`; delay .68s + duration .1s = .78s. No timeout. Replay resets class and forces recalculation. Focus, scroll and geometry remain stable. Reduced motion completes immediately.
