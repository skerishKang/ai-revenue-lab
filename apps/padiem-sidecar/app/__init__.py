"""B53 Padiem Sidecar product application slice.

Product adapter and repository-local reference host only; all reusable
runtime primitives are imported from IP-SIDECAR (padiem_embedded_runtime).
"""
