[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "korean-ai-code-agent"
version = "0.1.0"
description = "Business 54 Korean-first permission-gated coding-agent CLI vertical slice"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
kagent = "kagent.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
