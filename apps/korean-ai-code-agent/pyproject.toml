[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "korean-ai-code-agent"
version = "0.2.0"
description = "Business 54 Padiem Claw cloud-ready task, run, and sandbox boundary foundations"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
kagent = "kagent.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
