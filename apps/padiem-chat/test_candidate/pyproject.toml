[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "padiem-chat-test-candidate"
version = "0.1.0"
description = "B62 TEST-only lightweight Worker candidate"
requires-python = ">=3.11"
dependencies = [
  "starlette>=0.37,<1",
  "httpx>=0.27,<1",
]

[tool.pywrangler]
allow-build = false
