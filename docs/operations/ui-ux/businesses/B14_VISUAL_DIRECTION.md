# B14 — Korean AI Platform Visual Direction

Status: `DIRECTION_FROZEN`  
Verdict: `KEEP`

Preserve the current functional execution workspace and mobile connection/model/API-first journey. Only focused polish is allowed if later conformance review finds a bounded defect.

`OWNER_UI_APPROVED=false` remains unchanged.

## Evidence

- fresh platform-family audit run `31422928265`, artifact `9076118820`
- canonical runtime `https://ai-revenue-korean-ai-platform.charliekant.workers.dev/workspace`
- recent B14 mobile journey correction already placed connection/model/API controls before the session workspace.

Fresh screenshots show a real dense developer/AI execution workspace rather than a generic landing poster. Its density and dark utility surface are justified by the product job.

## Product thesis

A Korean-first operator connects an execution environment, selects/configures model/API context and works inside a bounded session with clear operational state.

```text
CONNECT → CONFIGURE → EXECUTE → INSPECT SESSION
```

Core object: **the live execution/session workspace and its connection/model state**.

## Reserved territory

**Korean AI Execution Workspace**

- dense operational controls
- visible connection and model/API status
- Korean-first labels and practical hierarchy
- session workspace as actual work surface
- restrained developer-console styling

Avoid marketing hero, model-logo wall, generic chat-only interface and B42 control-tower overview language.

## Preserve

- current desktop workspace density
- mobile ordering of connection/model/API before session body
- clear operational state
- backend/runtime contracts
- Korean-first utility copy

## Differentiation

- B32 = building/reviewing reusable skills; B14 = running a configured AI session.
- B42 = portfolio of development work/evidence; B14 = one active execution workspace.
- B53 = embedding SDK contract; B14 = end-user/operator runtime.

## Acceptance criteria

1. real workspace remains visible immediately;
2. connection/model/API state precedes action where necessary;
3. Mobile chrome does not cover session work;
4. no redesign into a giant-title product poster;
5. dense UI remains legible and Korean-first;
6. existing runtime/state contracts stay intact.
